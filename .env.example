# Optional: only needed if you run test-api.js or test-e2e.js from the command line.
# The app gets the token from the welcome screen (browser); it does not read this file.
# Copy to .env and set SONAR_TOKEN if you want to run: node test-api.js

SONAR_TOKEN=your_token_here
